# .env.example
# This file serves as a template for the required environment variables.
# Copy this file to a new file named .env and fill in the values.
# Do NOT commit the .env file to version control.

# --- Application Configuration ---
# The port the server will run on.
PORT=8000
# The URL of the frontend application for CORS configuration.
FRONTEND_URL=http://localhost:3000

# --- Database Configuration ---
# Your MongoDB connection string.
DB_URI=mongodb://localhost:27017/intranet-mvp

# --- Security & Authentication ---
# Secret key for signing JWT access tokens. Use a long, random string.
JWT_SECRET=your_jwt_secret_key
# Secret key for signing JWT refresh tokens. Should be different and more complex than JWT_SECRET.
JWT_REFRESH_SECRET=your_jwt_refresh_secret_key

# --- Network Security ---
# Comma-separated list of IP addresses that are allowed to access the API.
# For local development, include ::1 (for localhost IPv6) and 127.0.0.1 (for localhost IPv4).
WHITELISTED_IPS=::1,127.0.0.1
# (Optional) Assign friendly names to whitelisted IPs. Format: "ip1:Name One,ip2:Name Two"
IP_NAME_MAPPING=::1:Local Development,127.0.0.1:Local Development

# --- Third-Party Services ---
# (Optional) Your API key from virustotal.com for scanning file uploads.
# If left blank, virus scanning will be skipped.
VIRUSTOTAL_API_KEY=

# --- Google Cloud Storage (Optional) ---
# If you want to use GCS for avatar uploads, fill these in.
# If left blank, avatars will be saved to the local server disk.
GCS_PROJECT_ID=
GCS_BUCKET_NAME=
GCS_KEYFILE_PATH=./gcs-keyfile.json

# --- Queue Configuration (Redis) ---
# Required for asynchronous jobs like virus scanning.
REDIS_HOST=127.0.0.1
REDIS_PORT=6379